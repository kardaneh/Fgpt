initialization:
     33, Generating update device(check_cwrr,mc(:,:,:),dz(:))
main:
     23, Generating enter data copyin(rudr_corr(:,:),njsc(:),check(:,:),mcs(:),is_over_mcs(:))
     24, Generating NVIDIA GPU code
         25, !$acc loop gang, vector(128) ! blockidx%x threadidx%x
     24, Generating implicit copy(is_over_mcs(:)) [if not already present]
         Generating implicit copyin(mcs(:)) [if not already present]
         Generating implicit copy(check(:,:),rudr_corr(:,:)) [if not already present]
         Generating implicit copyin(njsc(:)) [if not already present]
     29, Generating update self(mc(:,:,:),rudr_corr(:,:),check(:,:),is_over_mcs(:))
     30, Generating exit data delete(rudr_corr(:,:),njsc(:),check(:,:),mcs(:),is_over_mcs(:))
hydrol_soil_smooth_over_mcs2_acc:
     99, Generating acc routine seq
         Generating NVIDIA GPU code
